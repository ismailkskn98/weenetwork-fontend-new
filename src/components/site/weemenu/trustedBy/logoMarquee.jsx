"use client";

import Integrations from "@/components/ui/integrations";

export default function LogoMarquee({ items }) {
  return <Integrations items={items} className="opacity-55 lg:opacity-70" />;
}
